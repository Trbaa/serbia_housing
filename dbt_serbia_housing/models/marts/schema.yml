version: 2

models:
  - name: fct_nekretnine
    description: >
      Gold sloj. Staging plus lokacija (seed lookup) i amenity
      booleani izvedeni iz opisa.
    columns:
      - name: unified_id
        tests: [unique, not_null]
      - name: izvor
        tests: [not_null]
      - name: lokacija
        tests: [not_null]